import {
  buildVIPSettingsPayload,
  createDefaultVIPSettings,
  createEmptyVIPBenefit,
  createEmptyVIPLevel,
  createEmptyVIPTask,
  normalizeVIPSettings,
} from "@infinitech/admin-core";

export const DEFAULT_VIP_SETTINGS = createDefaultVIPSettings();

export {
  buildVIPSettingsPayload,
  createDefaultVIPSettings,
  createEmptyVIPBenefit,
  createEmptyVIPLevel,
  createEmptyVIPTask,
  normalizeVIPSettings,
};
