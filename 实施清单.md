# 实施清单

这份清单只记录当前“管理端盘活 + 平台统一收款自研分账 + 全端真实支付/充值/提现 + 骑手保证金 + 银行卡提现方案”的真实落地状态。做完的内容会从待完成里移除，未完成的内容会继续往下收。

## 已完成

### 管理端与导航
- 管理端导航已经升级为“分组 -> 小节 -> 菜单项”的三层结构，保留了现有骨架，没有推倒重做。
- 财务组已新增“支付中心”，旧“付款说明管理 / PaymentInfo”入口已下线。
- 原空白占位页已盘活为“联调工作台”。
- 顶部面包屑和导航结构已同步细化。

### 支付中心
- 已支持分端分场景支付矩阵配置。
- 已支持提现手续费规则配置。
- 已支持分账对象、分账规则和规则预览。
- 已支持骑手保证金策略配置。
- 已支持银行卡提现 `sidecar / provider` 配置。
- 已支持支付回调与出款日志查询：
  - 可按渠道、事件、处理状态、验签状态筛选。
  - 可按关联交易号、第三方单号精确筛选。
  - 可查看关联交易、关联提现单、请求头、请求体、响应体详情。
- 已支持“订单分账查询”：
  - 可按订单 ID / UID / TSID / 日订单号查询清分快照。
  - 可查看分账分录、订单概览与快照 JSON。
- 已增加配置保存时的后端校验：
  - 用户端不允许银行卡提现。
  - 小程序不允许支付宝支付/充值。
  - `rider_deposit` 仅允许骑手 App 的微信/支付宝。
  - 已启用提现渠道必须存在手续费规则。
  - 手续费区间不允许重叠。

### 后端账务主链
- 订单支付、充值、保证金支付已改成第三方渠道状态流，不再“提交即成功”。
- 提现已改成统一状态机：
  - `pending_review`
  - `pending_transfer`
  - `transferring`
  - `success`
  - `failed`
  - `rejected`
- 订单退款已改成：
  - `refund_pending -> callback success -> refunded`
- 提现状态查询已支持主动查单补偿。
- 微信 / 支付宝提现自动对账 worker 已接入服务启动链。
- 支付回调已补齐幂等和乱序保护。
- 后台提现处理已支持：
  - 通过
  - 驳回
  - 发起打款
  - 同步状态
  - 失败后重试打款
  - 补记成功
  - 补记失败

### 分账系统
- 已落地结算对象、规则集、规则步骤、订单清分快照、结算分录模型。
- 已支持四种规则：
  - `percent_of_gross`
  - `tiered_by_order_amount`
  - `fixed_amount`
  - `remainder`
- 已接入主链：
  - 支付成功生成待分账快照
  - 订单完成正式入账
  - 退款执行分账冲销

### 骑手保证金
- 已支持 50 元保证金策略。
- 已接入“未缴保证金不可接单”的基础门禁。
- 已接入“7 天未接单且无进行中订单可提现”的基础判断。
- 保证金提现已并入统一提现链路。
- 保证金自动提现时已支持自动发起打款。

### 银行卡提现
- 银行卡提现仅开放给骑手端和商户端。
- 银行卡提现到账时效已固定展示为“24 小时 - 48 小时”。
- 银行卡提现申请时会先冻结金额，并纳入手续费结算。
- 管理端已支持银行卡提现人工处理闭环：
  - 待处理列表
  - 人工打款完成
  - 驳回并返还冻结金额
  - 上传凭证
  - 保存详细出款来源信息
- 打款凭证和出款来源仅管理端可见。
- 银行卡回调验签路径和 sidecar 健康检查已接入。

### 客户端资金体验
- 用户小程序、用户 App、骑手端、商户端的钱包充值 / 提现已接入状态轮询。
- 订单确认页已按后台支付能力动态显示支付方式。
- 三端账单详情已接入单笔交易状态查询。
- 后端已新增增强版提现选项输出，会按渠道返回：
  - `arrivalText`
  - `accountPlaceholder`
  - `accountHint`
  - `namePlaceholder`
  - `requiresName`
  - `requiresBankName`
  - `requiresBankBranch`
  - `bankNamePlaceholder`
  - `bankBranchPlaceholder`
  - `reviewNotice`
- 后端已新增银行卡提现字段校验：
  - 必须填写收款人姓名
  - 必须填写开户银行
  - 必须填写开户支行
- 用户小程序提现页已重写，去掉重复方法并接入动态字段提示。
- 用户 App 提现页已重写，去掉重复方法并接入动态字段提示。
- 骑手端提现页已重写，银行卡提现会按后端规则收集持卡人、开户行、开户支行。
- 商户端钱包页已重写，提现流程不再只收银行卡号，现已完整收集：
  - 收款账户
  - 收款人姓名
  - 开户银行
  - 开户支行

### 本地校验
- `go build ./cmd`
- `go test ./...`
- `node --check backend/alipay-sidecar/server.js`
- `node --check backend/bank-payout-sidecar/server.js`
- `cmd /c npm run build`（`admin-vue`）
- `cmd /c npm run lint`（`backend/bff`）

## 待完成

### 真实 SDK 与资金闭环收尾
- 银行卡提现仍未接入真实银行或真实三方代付供应商，当前仍是 `sidecar / allow_stub / manual-bank-batch` 方案，不能视为真实资金通道完工。

### 真机联调
- 用户小程序真实微信支付联调未完成，需要真实小程序、商户号和回调公网环境验收。
- 用户 App 真实微信 / 支付宝支付联调未完成，需要真机、真实应用签名与平台审核配置验收。
- 骑手 App 真实充值 / 提现联调未完成，需要真机账号与真实通道配置验收。
- 商户 App 真实充值 / 提现联调未完成，需要真机账号与真实通道配置验收。

### 管理端继续盘活
- 管理端源码级路由映射与乱码检索已补扫，剩余仍需浏览器全路由人工巡检确认运行时白屏与配置错误页。

## 当前最关键的下一步
1. 接入银行卡真实异步出款通道，替换当前 `allow_stub / manual-bank-batch` 兜底链路。
2. 完成用户端、骑手端、商户端的真机支付与提现联调验收。
3. 继续做管理端全路由人工巡检，确认没有残余白屏、配置错误页和历史乱码。

## 2026-04-02 本轮续推进
- 提现失败后的自动重试元数据已接入提现状态接口，管理端支付中心和联调工作台都能直接看到“待自动重试”状态。
- 提现处理队列已新增“自动重试待执行”统计，便于运营判断哪些失败提现会由系统继续补偿。
- 提现执行阶段如果第三方网关立即返回失败，系统会立刻回滚冻结金额、把提现单改回失败，并写入下一次自动重试计划。
- 新增回归测试覆盖：
  - 提现状态返回 `autoRetry` 元数据
  - 提现执行即时失败后的余额回滚与重试计划
  - 对账 worker 状态快照里的 `retryPendingCount`
- 新增“回调日志”能力：
  - Go 后端新增支付回调列表和详情接口
  - BFF 新增 `/api/admin/wallet/payment-callbacks` 及详情代理
  - 支付中心新增“回调日志”页签与详情抽屉
  - 回归测试覆盖回调日志筛选、详情解析和关联提现单信息
- 新增“订单分账查询”能力：
  - 支付中心可按订单号查询清分快照和结算分录
  - 可查看订单概览、快照状态、分录列表和快照 JSON
  - 回调日志筛选已补充“关联交易号 / 第三方单号”精确检索
  - 已新增回归测试覆盖“有快照 / 无快照”两种订单分账查询场景
- 新增“回调重放处理”能力：
  - Go 后端新增 `/api/admin/wallet/payment-callbacks/:id/replay`
  - BFF 已新增回调重放代理
  - 支付中心的回调日志列表和详情抽屉都可直接重放“已验签”回调
  - 回调列表和详情已可显示“是否后台重放 / 来源回调号 / 重放管理员”
  - 回归测试已覆盖“重放成功推进提现状态”和“拒绝重放未验签回调”
- 新增“提现处理轨迹”能力：
  - 后端提现审核、执行、补单、重试、同步网关状态都会写入 `admin_wallet_operations`
  - `ListAdminOperations` 已支持按 `transactionId` 精确筛选
  - 支付中心新增“处理轨迹”弹窗，可直接查看单笔提现的处理时间、动作、处理人、说明和备注
  - 回归测试已覆盖“审核通过自动记轨迹”和“按 transactionId 过滤轨迹”

## 2026-04-03 本轮续推进
- 继续补提现真实回调闭环：
  - 已补齐按 `withdraw request id / business_id` 反查钱包交易的兜底逻辑，避免微信提现、支付宝提现回调只带业务单号时“验签成功但落不到提现单”。
  - 已补齐提现执行、主动查单补偿、异步成功/失败回调、失败重试重置几个阶段对 `wallet_transactions.third_party_order_id` 的同步，避免提现单和钱包交易表的第三方单号脱节。
  - 已补齐 `rider_deposit_withdraw` 的回调结算分支，保证骑手保证金提现也能走统一的异步成功 / 失败回写链路。
- 已修正回调状态识别：
  - `DEALING`、`transferring` 等中间态不再误判成成功，会继续保持 pending / transferring 等待后续回写或主动查单补偿。
  - `error`、`deny`、`timeout` 等失败态已纳入统一失败识别。
- 新增回归测试覆盖：
  - 支付宝提现回调返回 `DEALING` 时保持处理中，不误推进成功。
  - 提现回调仅携带 `requestId` 时，仍可正确命中提现钱包交易并完成落账。
  - 骑手保证金提现可通过异步回调完成成功结算。
  - 提现执行进入 `transferring` 后会同步把第三方单号写入 `wallet_transactions`。
- 已继续补齐回调日志关联：
  - 新写入的支付 / 提现回调会优先绑定真实钱包交易 ID，不再把 `requestId / out_biz_no / out_batch_no` 这类业务单号误存成独立交易引用。
  - 历史回调日志在查询详情时，已支持按 `transaction_id_raw / business_id / third_party_order_id` 回补关联真实交易与提现单。
  - 回归测试已覆盖“回调记录写入真实交易引用”和“历史回调仅保留业务单号时仍可关联提现单”。
- 本轮已执行本地校验：
  - `go test ./...`
- 已继续补齐回调重放兼容：
  - 后台重放已优先沿用 callback 记录里的 `transaction_id_raw`，避免历史回调的 `transaction_id` 是脏值时重新落单失败。
  - 回归测试已覆盖“历史回调仅在 `transaction_id_raw` 保留 requestId / business_id 时，仍可重放成功并绑定真实提现交易”。
- 已继续收口主动查单 / 补单链路：
  - 后台同步网关状态、提现状态主动查单、reconcile worker 现在都会先判断提现单是否真的已经提交到网关，避免还停留在 `pending_transfer` 审批态的单子被误查单、误记一次“同步成功”。
  - 对已经提交网关但本地仍停在 `pending_transfer` 的提现单，主动查单拿到 pending / transferring 类状态后会自动推进到 `transferring`，不再卡在“已出款但本地还像未执行”的中间态。
  - 主动查单写回 `wallet_transactions.response_data` 时已改成 merge 模式，会保留 `submittedAt / autoRetry / retryCount` 等旧上下文，同时补入 `gatewayStatus / lastQueryAt / integrationTarget / thirdPartyOrderId / transferResult`，避免补偿查单把原始提交信息覆盖掉。
  - 回归测试已覆盖“未提交网关的 pending_transfer 不会被 reconcile worker 误轮询”和“已提交 pending_transfer 经同步网关状态后正确推进并保留原提交 / 重试元数据”。
- 已继续打通提现列表增强字段：
  - `ListWithdrawRecords` 现在会补齐 `auto_retry / response_data / transaction_status / gateway_submitted / arrival_text` 等增强字段，支付中心和联调工作台里之前依赖这些字段的 UI 不再拿到空值。
  - 支付中心“同步状态”按钮已改成只对真正提交过网关的微信提现 / 支付宝 / 银行卡提现展示，避免点开后才报“未提交网关”。
  - 回归测试已覆盖“提现列表返回自动重试 / 网关提交态增强字段”和“未提交网关的待打款单不会显示错误的同步状态前提”。
- 已继续补齐历史列表兜底与管理端路由复核：
  - `ListWithdrawRecords` 对缺失 `transaction_id` 的历史提现单已支持按 `request_id / third_party_order_id` 反查钱包交易，增强字段不再只对新数据生效。
  - 管理端菜单和受保护路由映射已复核，当前菜单项都有显式页面映射；`BlankPage` 仅保留首页与联调工作台入口，源码级乱码检索未再发现新增残留。
  - 回归测试已覆盖“历史提现单缺失 transaction_id 时仍返回增强字段”。
- 已继续补齐前台真实支付拉起链路：
  - 用户小程序、用户 App、骑手 App、商户 App 的订单支付 / 钱包充值 / 骑手保证金缴纳现在都会消费后端返回的 `paymentPayload`，按微信 / 支付宝参数真正调用客户端支付桥，不再只停留在“请求已提交”的提示。
  - 用户端和 App 订单确认页已补齐支付后交易状态轮询，只有真正确认成功才进入支付成功页；取消或待确认时会回到订单详情继续跟进。
  - 用户端和 App 订单详情、订单列表已补齐 `pending_payment` 的“继续支付”入口，解决用户取消三方支付后订单留在待支付却没有继续支付出口的问题。
  - 骑手保证金缴纳已补齐客户端支付拉起后的状态确认，不再只凭提交结果即时 toast 判定。
- 已继续补齐全局实时通知主链：
  - Go 后端已新增统一实时通知服务，业务事件现在可同时投递到移动推送队列和 socket-server 广播入口。
  - socket-server 已新增 `/notify` 命名空间与 `/api/realtime/publish` 后台广播接口，按 `user / rider / merchant / admin` 房间分发实时业务事件。
  - 用户小程序、用户 App、骑手 App、商户 App 已新增全局实时通知桥，登录后会常驻连接 `/notify`，收到业务事件后可即时 toast，并向页面广播 `realtime:notification / realtime:refresh:*` 事件。
  - 订单列表、商户订单列表、骑手大厅、商户钱包已补齐关键实时刷新监听；支付回调、提现审核/完成/失败、售后创建/状态更新、订单派单/接单/取货/送达等核心事件已接入服务端主动通知。
  - 提现驳回原因已补充透传到前台状态接口，用户/骑手/商户侧会明确看到“提现已驳回”及原因，而不是只看到笼统失败。
- 本轮已补充前后端校验：
  - `go test ./...`
  - `cmd /c npm run build`（`admin-vue`）
  - `cmd /c npm run lint`（`backend/bff`）
  - `node --check backend/alipay-sidecar/server.js`
  - `node --check backend/bank-payout-sidecar/server.js`
  - `helper-import-ok`：已通过源码级 ESM 导入校验（前台支付桥 helper）
  - `page-logic-parse-ok`：已通过订单详情 / 订单列表 JS 语法编译校验
  - `vue-script-parse-ok`：已通过用户端 / App 端 / 骑手端相关 `.vue` 脚本块语法编译校验
  - `socket-server`：`cmd /c npm run check` 已通过
  - `realtime-common-parse-ok`：已通过实时通知共用 helper 源码语法编译校验
  - `rider-app` 的 `npm run build:app` 在当前环境未能直接执行：本机缺少可调用的 `uni` 命令，需在完整 uni-app 构建环境中继续做真机构建验收。
