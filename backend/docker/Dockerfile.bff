# Node.js BFF Dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制 package 文件
COPY bff/package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY bff/ ./

# 创建日志目录
RUN mkdir -p logs

# 暴露端口
EXPOSE 25500

# 启动服务
CMD ["npm", "start"]
